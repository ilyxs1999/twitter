import { StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container : {
      flex : 1,
  },
  text : {
    alignSelf : "center",
    fontSize : 20,
    marginTop : 10,
    marginBottom : 10
  },
});